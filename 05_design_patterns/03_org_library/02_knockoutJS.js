// Model View ViewModel (i.e. controller; seperates Model from View)

// Declarative bindings (formerler, Data <-> Controller <-> DOM; now we put
// the logic in the DOM in the form of bindings)

// Automatic UI (user interface) Refresh (when data/model changes)

// Dependency tracking - models can depend on other models

// example code: http://jsfiddle.net/rniemeyer/3Lqsx/
